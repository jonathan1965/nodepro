// npm - global command , comes with node 
// npm, --version


// local depenency - use it only in this particular project
//  npm install (packagename) -> npm i

//  global depency - use in any project
// npm install -g <packageName>
// sudo install -g <packageName> (mac)

// package.json - manifest file (store important info about project/package)
// manually approcah (create package.json in the root)
// npm init (step by step, press enter skip)
// npm init -y (everything default)


const jo = require('lodash')

const items = [1,[2,[3,[4]]]]
const newItems = jo.

console.log(newItems)